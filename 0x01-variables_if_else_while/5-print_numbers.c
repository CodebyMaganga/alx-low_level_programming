#include <stdio.h>
/**
 * main- prints all single digits of base 10
 * Return: 0 if succesfull
 */
int main(void)
{
	int digit;

	for (digit = 0; digit < 10; digit++)
		printf("%d\n", digit);
		return (0);
}
