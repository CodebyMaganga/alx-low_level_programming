#include "main.h"
/**
 * _isupper- Checks for uppefrcase letters
 * @c: variable
 *
 * Return: 1 if succesful
 */
int _isupper(int c)
{
	if (c == 'A' && c <= 'Z')

		return (1);
	else
		return (0);
}
