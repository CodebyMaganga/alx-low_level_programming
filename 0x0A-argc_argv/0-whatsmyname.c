#include "main.h"
#include  <stdio.h>

/**
 * main- prints its own name
 * @argc: argument count
 * @argv: array of arguments
 * Return: 0 if succesful
 */

int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);

	return (0);
}

